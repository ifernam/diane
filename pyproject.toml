[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "diane"
version = "0.1.0"
dependencies = [
    "tzlocal",
    "networkx",
    "pyyaml",
    "typer"
]

[tool.setuptools.packages.find]
where = ["src"]